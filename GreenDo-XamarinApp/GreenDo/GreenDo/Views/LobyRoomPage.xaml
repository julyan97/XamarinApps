<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="GreenDo.Views.LobyRoomPage"
             BackgroundImageSource="baselayout.png"
             NavigationPage.HasNavigationBar="False"
             NavigationPage.HasBackButton="True">

    <StackLayout>

        <StackLayout Margin="0, 250 ,0 ,0 " HorizontalOptions="Center" VerticalOptions="Center">
            <Button Command="{Binding RedirectToCameraCommand}}" HeightRequest="100" WidthRequest="180" Text="Become Active" Margin="0,0,0,50" CornerRadius="15">
            </Button>
            <Button Command="{Binding RedirectToFeedCommand}" Text="Feed" CornerRadius="15" HeightRequest="100" WidthRequest="100"></Button>
        </StackLayout>
    </StackLayout>
  
</ContentPage>